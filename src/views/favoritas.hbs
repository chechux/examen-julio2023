<div class="container p-4">
    <div class="row">
        {{#each pelis}}
            <div class="col-md-3">
                <div class="card text-center">
                    <div class="card-body">
                            <h3 class="card-title text-uppercase">
                                {{titulo}}
                            </h3>
                        </a>
                        <p class="m-2">{{description}}</p>
                        <p></p>
                        <a href="/desfavoritos/{{id}}">quitar de favoritos</a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>